# Seasons — Gap Técnico (F4)

**Status:** `GetSeasonBoardsState()` implementado (`sources/economy/EconomyService.gd`);
`Season pass` (`FEATURE_MATRIX.md` §3) implementado;
Mas **snapshot de temporada** + **4 corridas** (`Power`, `Boss Kills`, `Spend`, `Guild Points`) não implementadas.
**Pré-requisito:** decisão de design das regras congeladas + changelog público (`ROADMAP.md` §F4).
**Dependência:** dono decide se inclui no lançamento ou pós-lançamento.
